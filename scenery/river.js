var River = function(x, y, m, c){
	this.x = x;
	this.y = y;
	this.m = m;
	this.c = c;
	
	this.sections = function()
	{
		var intersectX = {x: -this.c / this.m, y: 0;
		var intersectY = {x: 0, y: y};
		var intersect = {x: 0, y: 0};
		
		return [
			//river
			[{type:"set",x:x,y:y},]
		];
	}
	
	return this;
}